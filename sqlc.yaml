version: '2'
cloud:
  project: '01HJ7CN1EWXH4352QQM7EFTTS1'
sql:
- schema: migrations
  queries: queries
  engine: postgresql
  gen:
    go:
      package: "db_new"
      out: "internal/db_new"
      sql_package: "pgx/v5"
  # The following lines are only required
  # if you're using managed databases with
  # sqlc generate
  database:
    managed: true
